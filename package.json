{
  "name": "kochbuch",
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "db": "sst shell drizzle-kit",
    "better-auth:generate": "bunx @better-auth/cli generate --config ./packages/core/src/api/auth/auth.ts --output ./packages/core/src/db/schemas/auth.ts"
  },
  "devDependencies": {
    "@tsconfig/node22": "^22",
    "prettier": "^3.6.2",
    "typescript": "^5"
  },
  "dependencies": {
    "drizzle-kit": "^0.31.5",
    "sst": "3.17.13"
  },
  "trustedDependencies": [
    "aws-sdk"
  ]
}
