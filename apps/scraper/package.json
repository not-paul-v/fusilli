{
  "name": "scraper",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile scraper",
    "dev": "alchemy dev",
    "deploy": "alchemy deploy",
    "destroy": "alchemy destroy"
  },
  "dependencies": {
    "hono": "^4.8.2",
    "dotenv": "catalog:",
    "zod": "catalog:",
    "@cloudflare/puppeteer": "^1.0.4"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "tsdown": "catalog:",
    "alchemy": "^0.70.0",
    "wrangler": "^4.40.3",
    "@types/node": "^22.13.11",
    "@cloudflare/workers-types": "^4.20250822.0"
  }
}
